---
"@trokky/fields": patch
"@trokky/studio": patch
---

UI improvements for array and object fields

- Fixed chevron rotation for nested ObjectField components in arrays by updating Tailwind config to scan fields package
- Added visual indication (blue border and background) for array/object items when their modal is open
- Cleaned up array field rendering by removing problematic auto-open and auto-focus features
