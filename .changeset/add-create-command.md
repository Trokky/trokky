---
"@trokky/trokky": patch
---

Add `trokky create` command for project scaffolding with interactive prompts and CLI flags. Supports minimal, full, and api-only templates with configurable data/media adapters, mail providers, auth modes, and studio options. Generates README.md with setup instructions, .npmrc for GitHub Packages registry, and detects NODE_AUTH_TOKEN for seamless authentication.
